image: alpine:latest

pages:
  stage: deploy
  script:
  - mkdir public
  - cp -r dashboard.css dashboard.js index.html Leaflet.Icon.Glyth.js img public/
  artifacts:
    paths:
    - public
  only:
  - master
